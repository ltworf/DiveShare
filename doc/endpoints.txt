dive_id
dive_delete_id


Upload dives
============

```
curl -XPUT http://server/upload?private=true -T file.js
```

Returns lines containing the dive_id and the dive_delete_id
separated by a comma.

Lines are \n separated, with a trailing \n
